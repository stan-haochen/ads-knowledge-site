// Original teaching exposition: mathematical derivations, interface conventions,
// and examples are written for this course; primary references are linked below.
export const contents=[
 ['model','01 · 模型与势能'],['rotation','02 · 三种旋转的证明'],['access','03 · 访问引理与序列'],['operations','04 · 操作势能实验'],['search','05 · 查找与边界'],['split','06 · Split'],['join','07 · Join'],['insert','08 · 两种插入'],['delete','09 · 删除'],['augment','10 · 排名与区间'],['bulk','11 · 批量操作与实现'],['weighted','12 · 带权推论'],['audit','13 · 证明检查与练习']
];
export const summaryRows=[
 ['Splay(x) / 成功查找','一次伸展','O(log(n+1))','x 最终到根'],['失败查找 / 重复插入 / 删除缺失键','伸展最后访问节点或命中节点','O(log(n+1))','失败也调整'],['Min / Max','走到端点，再伸展','O(log(n+1))','返回根即是答案'],['Split','查找伸展 + 断一条边','O(log(n+1))','明确 < / ≤ 约定'],['Join(L,R)','伸展 max(L) + 接 R','O(log(|L|+|R|+1))','max(L) < min(R)'],['Insert / Delete / 修改键','常数次基本操作','O(log(n+1))','本页集合语义，节点单位权'],['前驱 / 后继 / lower_bound','Split + 端点 + Join','O(log(n+1))','无答案时仍恢复树'],['Rank / Select','按 size 搜索 + 伸展','O(log(n+1))','每次旋转 O(1) 更新 size'],['区间统计 / 合法懒更新','两次 Split + 合并','O(log(n+1))','每次 push / pull / 标记合成 O(1)'],['区间报告 k 个元素','隔离 + 中序 + 合并','O(log(n+1)+k)','必须计输出成本'],['全树遍历 / 释放 n 个节点','显式栈 / 逐节点释放','Θ(n) 实际','无需对每个节点单独伸展'],['清空根引用 / 读取缓存大小','一次指针赋值 / 字段读取','O(1) 实际','清空不含内存回收成本']
];
const eq=s=>`<div class="sp-equation">${s}</div>`;
export const sections={
 model:`<p>以下讨论<strong>自底向上（bottom-up）Splay</strong>、互异键集合、可修改的树。一次完整 Zig-zig 必须先把父亲 p 转过祖父 g，再把 x 转过 p；Zig-zag 则把 x 连续转过 p、g。单次操作可以花 Θ(n)，本文证明的是对任意合法操作序列成立的<strong>确定性均摊上界</strong>，不需要输入随机。</p>
 <p>先给每个存活节点一个固定正权 w(v)。分析权重不必存储在实现中；它与用于顺序统计的 size 是两回事。对一棵树或一个森林定义：</p>${eq('s(v) = Σ_{u ∈ subtree(v)} w(u)\nr(v) = log₂ s(v)\nΦ(F) = Σ_{T ∈ F} Σ_{v ∈ T} r(v)\nĉ = c + ΔΦ = c + Φ_after − Φ_before')}
 <p>局部旋转证明中 c 只计旋转：Zig 为 1，双旋为 2。断边、接边、分配节点还需常数实际工作；图中会明确列出它们的势能变化。查找深度为 d 时，伸展恰好旋转 d 次，所以沿途比较和父指针更新都是 O(c+1)。若真实工作 a ≤ K(c+b)，其中 b 是常数个结构步骤，则用放大后的势能 Ψ=KΦ，得到 a+ΔΨ ≤ K(c+ΔΦ+b)。<strong>这一步才把旋转计数转换成真实时间。</strong></p>
 <p>涉及插入、删除的基本操作证明统一取 w(v)=1，因此 r(v)=log₂|subtree(v)| ≥ 0，空森林势能为 0。若森林总节点数不超过 N，则 0 ≤ Φ ≤ N log₂ max(1,N)。固定带权集合的 Φ 可能为负，不能直接套用这个非负性；第 12 节单独处理。</p>
 <p>势能只用于证明，不要求执行时重新遍历树求和。图示为了显示所有节点的 s、r、Φ 而额外计算的工作，不属于数据结构算法的运行时间。</p>`,
 rotation:`<p>图中 A、B、C、D 是内部结构完全不变的子树，可为空；圆形 x、p、g 是旋转涉及的节点。撇号表示完整轮结束后的量。局部子树之外的祖先仍包含同一个节点集合，所以秩不变；A–D 内每个节点的子树也不变。只需要计算 x、p、g。</p>
 <h3>唯一需要的不等式：两个不相交集合</h3><p>对 a,b>0，有 (a−b)² ≥ 0，故 4ab ≤ (a+b)²。若 a+b ≤ S：</p>${eq('log₂a + log₂b ≤ 2 log₂(a+b) − 2 ≤ 2 log₂S − 2')}
 <p>减掉的 2 正好抵消双旋的两单位旋转成本。这里 a、b 将取<strong>包含节点本身的完整子树权重</strong>，所以即使 A–D 为空也严格为正。</p>
 <h3>Zig：父亲就是根</h3><p>以 x 为 p 的左孩子为例。新根 x 拥有原来 p 的整棵子树，因此 r′(x)=r(p)。又因为新 p 在新 x 内，r′(p)≤r′(x)。</p>${eq('ĉ = 1 + r′(x)+r′(p)−r(x)−r(p)\n  = 1 + r′(p)−r(x)\n  ≤ 1 + r′(x)−r(x)\n  ≤ 1 + 3[r′(x)−r(x)]')}
 <p>最后一步利用 x 的子树只会变大。一次 Splay 最多出现一个 Zig，且只能位于最后，因此这个 +1 不会按树深累加。</p>
 <h3>Zig-zig：x、p 同向</h3><p>以左左为例，先右旋 g，再右旋 p。r′(x)=r(g)，r(p)≥r(x)，r′(p)≤r′(x)。关键是<strong>旋转前 x 的子树</strong>与<strong>旋转后 g 的子树</strong>不相交：前者为 {x}∪A∪B，后者为 {g}∪C∪D；它们都包含在新 x 中。</p>${eq('s(x)+s′(g) ≤ s′(x)\nr(x)+r′(g) ≤ 2r′(x)−2\n\nĉ = 2 + r′(x)+r′(p)+r′(g)−r(x)−r(p)−r(g)\n  = 2 + r′(p)+r′(g)−r(x)−r(p)\n  ≤ 2 + r′(x)+r′(g)−2r(x)\n  ≤ 2 + r′(x)+[2r′(x)−2−r(x)]−2r(x)\n  = 3[r′(x)−r(x)]')}
 <p>此处的不相交集合跨越前、后两个状态。若把它误写成“旋转前 p 与 g 不相交”，证明会失效，因为它们是包含关系。</p>
 <h3>Zig-zag：x、p 反向</h3><p>以左右为例，先左旋 p，再右旋 g。此时<strong>旋转后 p 与 g 的子树</strong>不相交，均在新 x 内。仍有 r′(x)=r(g)、r(p)≥r(x)。</p>${eq('s′(p)+s′(g) ≤ s′(x)\nr′(p)+r′(g) ≤ 2r′(x)−2\n\nĉ = 2 + r′(p)+r′(g)−r(x)−r(p)\n  ≤ 2 + r′(p)+r′(g)−2r(x)\n  ≤ 2[r′(x)−r(x)]\n  ≤ 3[r′(x)−r(x)]')}
 <p>右右、右左情形是左右镜像，子树包含关系和每个式子完全相同。<strong>双旋的中间状态只解释指针，不能单独套用上述整轮不等式。</strong>这些计算证明的是所有正权重，而不只是面板中的有限数值案例。</p>`,
 access:`<h3>第一层望远镜：从轮次到一次 Splay</h3><p>设同一节点 x 在各轮前后的秩依次为 r₀(x),r₁(x),…,r_q(x)。前一轮结束就是后一轮开始；每轮使用同一个森林势能。x 最后成为原根 t 所在树的根，故 r_q(x)=log₂W，其中 W 是该树总权重。</p>${eq('Σ_j ĉ_j = C + Φ_after−Φ_before\n          ≤ 3Σ_j[r_j(x)−r_{j−1}(x)] + 1\n          = 3[log₂W−log₂s_before(x)] + 1\n          ≤ 3 log₂(W/w(x)) + 1')}
 <p>这就是带权访问引理。树外其他森林分量的势能没有变化。取单位权得到 C+ΔΦ ≤ 3log₂n+1；n=0 时直接返回、n=1 时无需旋转，真实成本统一写成 O(log(n+1))。</p>
 <div class="sp-telescope"><span>+ r₁ − r₀</span><span>+ r₂ − r₁</span><span>⋯</span><span>+ r_q − r_{q−1}</span><strong>= r_q − r₀</strong></div>
 <h3>第二层望远镜：从操作到整个序列</h3>${eq('Σ_{i=1}^m ĉ_i = Σ_i c_i + Φ_m−Φ₀\nΣ_i c_i = Σ_i ĉ_i + Φ₀−Φ_m')}
 <p>固定 n 个键、任意初始树，单位权旋转总数 ≤ m(3log₂n+1)+Φ₀−Φ_m。利用非负性只能去掉 −Φ_m，<strong>不能去掉 +Φ₀</strong>。因此这个证明给出 O(m log(n+1)+n log(n+1)) 的真实时间上界。一个深链上的第一次访问仍可能线性。</p>
 <p>若从空森林开始，执行 m 次本页的基本更新操作，整个过程中总节点数最多 N，则 Φ₀=0，各操作在同一森林势能下结算，得到 O(m log(N+1))，再加区间输出、遍历、回收等明确计费的工作。初始森林若非空，则加 O(N₀ log(N₀+1)) 的初始项。插删期间不能把每次势能都另行归零。</p>
 <p><a href="#avl/splay-potential">打开连续访问实验 ↗</a>：查看每次访问的真实旋转、势能变化和累计上界。</p>`,
 search:`<h3>成功与失败查找</h3><ol><li>沿 BST 比较路径走到目标 x。若成功，返回值先保存，再把 x 伸展到根。</li><li>若失败，保存最后一个非空节点 y，再把 y 伸展到根。失败路径只比到 y 的路径多一个空指针检查。</li><li>两种情况的搜索长度都由相应的伸展旋转数加常数控制，直接套用访问引理。</li></ol>
 <p>若失败时完全不伸展，反复查询一条长链之外的键可以每次都走 Θ(n)，树形不变、ΔΦ=0。这样的接口不具有这里证明的失败查询均摊 O(log n) 保证。</p>
 <h3>最小值、最大值</h3><p>沿 left 或 right 到端点，保存答案并伸展该节点。路径成本与旋转数成正比，访问引理给出 O(log(n+1))。如果只走路径而不调整，同样不能用本证明担保反复查询。</p>
 <h3>严格前驱、严格后继、lower_bound</h3><p>为使边界语义可审查，实验采用组合实现：前驱用 Split(&lt;k,≥k)，取左树最大值；后继用 Split(≤k,&gt;k)，取右树最小值；lower_bound 用 Split(&lt;k,≥k)，取右树最小值。保存答案后 Join 恢复集合。每种仅含常数次已证明的原语；空侧表示无答案。</p>
 <p>也可在一次搜索中保存候选并伸展，但必须证明搜索路径成本如何由伸展支付，不能只伸展一个很浅的候选、同时忽略之前走过的很长路径。这里选择组合版本以使证明与实验逐步一致。</p>`,
 split:`<p>定义 Split&lt;(T,k) 返回 (L,R)，L 含所有键 &lt;k，R 含所有键 ≥k。先执行成功或失败查找，把命中或最后访问节点 t 伸展到根。</p><ol><li>若 t.key ≥ k：令 L=t.left，断开 t.left，R=t。</li><li>若 t.key &lt; k：令 R=t.right，断开 t.right，L=t。</li><li>两个返回根的 parent 均置空，更新原根的 size。</li></ol>
 <p>为什么失败时可以这样切？失败搜索的最后节点必为 k 的紧邻前驱或后继：若最后从 t 的空右边退出，路径区间内不存在比 t 更大且仍小于 k 的键；空左边对称。旋转保持中序关系，所以伸展后的 t 一侧恰是分界所需集合。</p>
 <p>设伸展后整树大小为 n，切完仍以 t 为根的那一部分大小为 q（q≥1）。只有 t 的子树发生缩小，脱离的子树内部不变。</p>${eq('ΔΦ_cut = log₂q − log₂n ≤ 0\nĉ_split ≤ (3log₂n+1) + 0 + O(1) = O(log(n+1))')}
 <p>返回 ≤k 与 &gt;k 的 Split≤ 只需把判断改为 t.key≤k 时断右边，否则断左边。空树返回两个空树，势能为 0。<strong>两个结果作为森林共同继续记账；分裂不是两次重新初始化。</strong></p>`,
 join:`<p>前提是 L 的所有键严格小于 R。令 l=|L|、r=|R|、n=l+r。若有空树，直接返回另一棵树，ΔΦ=0。否则在 L 中搜索最大值 x 并伸展。</p><ol><li>设 x 伸展前的子树大小为 s₀(x)。访问引理只使用左树总量 l，而不是 n。</li><li>伸展后 x.right 必为空，否则 x 不是最大值。</li><li>令 x.right=R，R.parent=x，更新 x.size。</li></ol>
 <p>接边时只有 x 的子树从 l 变成 n；R 及其他节点内部结构均不变。因此可以给出比两次粗略 O(log n) 更清楚的式子：</p>${eq('ĉ_splay ≤ 3log₂(l/s₀(x))+1\nΔΦ_attach = log₂(n/l)\nĉ_join ≤ 3log₂(l/s₀(x))+1+log₂(n/l)+O(1)\n       ≤ 3log₂n+1+O(1)')}
 <p>最后使用 s₀(x)≥1、l≤n。接边虽然只写常数个指针，却可能<strong>增加</strong>对数势能，不能把它当作零均摊代价。任意两棵键区间交错的树不满足 Join 前提；一般集合并集不是这一个操作。</p>`,
 insert:`<h3>方案 A：普通 BST 叶插入，再伸展新节点</h3><p>这是常见的父指针实现。先搜索空位；若命中重复键，仅伸展命中节点并返回。否则创建单位权新叶 x，沿祖先更新 size，再把 x 伸展到根。</p><p>插叶前有 n 个节点。新叶的秩为 log₂1=0；只有搜索路径上的祖先从大小 s 变成 s+1。路径上旧子树大小严格递增，是 {1,…,n} 的一个子集 P。每项均非负：</p>${eq('ΔΦ_leaf = Σ_{s∈P} log₂((s+1)/s)\n         ≤ Σ_{s=1}^n log₂((s+1)/s)\n         = log₂(n+1)\n\nĉ_splay ≤ 3log₂(n+1)+1     [新叶伸展前 s(x)=1]\nĉ_insert ≤ 4log₂(n+1)+1+O(1)')}
 <p>路径虽然可能很长，不能按“每个祖先加 O(1)”粗算势能；上面用严格递增的大小得到整个路径的对数界。搜索、更新 size 和伸展都走 O(depth+1) 个步骤，用第 1 节的势能缩放计入实际成本。n=0 时插一个根，势能增量 0。</p>
 <h3>方案 B：Split 后把新节点作为根</h3><p>先查找排除重复键，再 Split&lt;(T,k) 得到 L、R；创建新根 x，连接左右孩子并更新 size。实验复用刚才的伸展结果，Split 阶段直接断边，不再重复搜索。</p>${eq('ΔΦ_new-root = log₂(n+1)\nĉ_insert-root = ĉ_search + ĉ_split + log₂(n+1) + O(1)\n               = O(log(n+1))')}
 <p>这里旧节点的子树完全未变，新增项只有新根的秩。不论采用哪种方案，遇到重复键都不新建第二个同键节点；多重集合需要明确是独立节点还是计数，不能直接把 n 的含义混用。</p>`,
 delete:`<p>采用“伸展目标 → 删除根 → Join 左右树”。搜索失败时，仅返回查找结果；成功时设当时树大小 n，左右子树 L、R 的大小为 l、r，l+r=n−1。</p><ol><li>第一次伸展：ĉ₁≤3log₂(n/s₀(x))+1。</li><li>把 L、R 的父指针置空，移除根 x。其余节点的子树大小没有变化，只有 x 的势能项消失。</li><li>若 L 或 R 为空，转交另一棵树；否则在 L 伸展 max(L)，再接上 R。</li></ol>${eq('ΔΦ_remove-root = −log₂n\nĉ_join ≤ 3log₂(l/s₀(y))+1+log₂((n−1)/l)   [l,r>0]\n\nĉ_delete ≤ 3log₂(n/s₀(x))+1 − log₂n\n           + 3log₂(l/s₀(y))+1 + log₂((n−1)/l) + O(1)\n         ≤ 6log₂n+2+O(1)')}
 <p>最后一行是便于记忆的宽松界：直接把第一次访问和 Join 各界为 3log₂n+1，丢弃非正的移除项即可。不要在 l=0 时计算 log l；n=1 时删除唯一节点，直接得到空树且势能不变。动画账本把删除根与接树分开显示，供逐项核对。</p>
 <p>逻辑删除和内存释放要区分：单节点析构假定 O(1)；若载荷析构本身昂贵，需要另计。修改一个键可用 Delete(old)+Insert(new)，新键冲突的行为由集合接口决定，总计 O(log(n+1)) 均摊。</p>`,
 augment:`<h3>Rank 和 Select</h3><p>维护 size(v)=1+size(v.left)+size(v.right)。旋转先 pull 下降的节点，再 pull 上升节点；每次 O(1)。Select(k) 比较 k 与 size(left)+1，下降到第 k 个节点后伸展。Rank(k) 统计小于 k 的键数：走右边时累加 size(left)+1，直到空指针，最后伸展最后访问节点。</p><p>两条路径的长度均由相应伸展的旋转数控制，因此同一单位权势能直接给出 O(log(n+1))。Select 的非法 k 可以先与根 size 比较，在 O(1) 内拒绝。不能每次递归重算 size；那样会改变真实运行时间。</p>
 <h3>区间报告和聚合</h3><p>闭区间 [a,b] 用 Split&lt;(T,a)=(L,U)，再 Split≤(U,b)=(M,R)。M 恰好包含区间。读取缓存 sum/min 等聚合量为 O(1)；报告全部 k 个键要中序遍历 Θ(k)。最后 Join(Join(L,M),R) 恢复集合。</p>${eq('ĉ_range-aggregate = ĉ_split1+ĉ_split2+O(1)+ĉ_join1+ĉ_join2\n                  = O(log(n+1))\nĉ_range-report = O(log(n+1)+k)')}
 <p>每个阶段都用整个森林的势能，内部状态相消。不把 M 误计为不存在，也不能在合并后重新给整树发一份初始势能。区间删除可隔离 M 后只 Join(L,R)；从森林移除 M 的势能非正，结构操作 O(log(n+1))，逐节点释放 M 另加 Θ(k)。</p>
 <h3>隐式 Splay 与懒标记：何时仍是 O(log n)</h3><p>隐式树按中序位置表示序列，用 size 做按位置 Split。隔离区间后，可在根记录翻转、加法等标记。需满足：标记合成、作用于聚合量、push、pull 都是 O(1)；旋转前自顶向下 push 路径，旋转后先下后上 pull。镜像翻转不改变各节点的子树节点数，因此单位权 Φ 不变；沿路径的 push 数可由伸展路径支付。</p><p>这说明<strong>合法增广</strong>的成本传递，不是任意更新都成立：对有序键树随意加值或翻转会破坏键顺序；字符串等可变长聚合若合并并非常数，也需乘上相应代价。可交互实验覆盖 Rank、Select 和区间报告，懒标记部分提供条件化证明。</p>`,
 bulk:`<h3>并非每个叫作“树操作”的过程都是 O(log n)</h3><ul><li>完整中序遍历用显式栈，每条边至多走常数次：Θ(n) 实际时间、O(h) 栈空间，h 可为 n。该过程不改树，ΔΦ=0。</li><li>清空根引用 O(1)，同时从逻辑森林移除整棵树使 ΔΦ=−Φ(T)≤0；若要求立即释放所有节点，必须另付 Θ(n)。</li><li>从已有有序数组中点递归建树：Θ(n) 实际时间。平衡树各层秩和为 O(n)，因此从空状态的建树均摊成本也为 O(n)。具体地，深度 d 至多 2ᵈ 个节点、子树大小 O(n/2ᵈ)，按距底部高度 j 重排，总和为 O(nΣ_{j≥0}(j+1)/2ʲ)=O(n)。</li><li>重复插入 n 个无序键：从空树得到 O(n log(n+1)) 总时间上界。一般集合 Union / Intersection 并非 Join；键区间若交错，就需要另一算法和证明。</li></ul>
 <h3>父指针实现的审查清单</h3><ol><li>旋转保存 g 和中间子树 β。先把 β 接回下降节点并设置 parent，再连接 x 与 p，最后正确更新 g 的孩子或根引用。</li><li>Zig-zig 先旋转 p，Zig-zag 先旋转 x；判断时必须先保存未改变的关系。</li><li>断树时清除新根 parent；Join 必须保留 R，且确认 max(L) 的 right 为空。</li><li>size 在旋转、插入回溯、断边、接边后都更新。理论上的固定权重不是随意改变的缓存。</li><li>深链可能导致递归栈溢出；生产实现优先用父指针迭代。图示快照的递归仅面向最多 40 节点的教学数据。</li></ol>
 <p><a href="#avl/implementation">对照第一课可运行的父指针旋转、查找与删除代码 ↗</a>。本专题实验引擎另实现 Split、Join、两种插入及顺序统计，用自动化测试检查结构与势能等式。</p>`,
 weighted:`<p>带权访问引理对任意<strong>固定正权</strong>成立，不表示算法读取这些权重。固定节点集合 V，W=Σw(v)。每个 r(v) 位于 log₂w(v) 与 log₂W 之间，即使 Φ 可能为负，仍有：</p>${eq('Φ₀−Φ_m ≤ Σ_{v∈V} log₂(W/w(v))\nΣ_i c_i ≤ Σ_i[3log₂(W/w(x_i))+1] + Σ_v log₂(W/w(v))')}
 <h3>带初始化项的频率界</h3><p>m 次成功访问中键 v 出现 f(v) 次。事后选择 w(v)=f(v)+1>0，则 W=m+n；不需要算法预知频率，也没有零权问题。按键合并访问项：</p>${eq('总真实成本 = O(m + Σ_v f(v) log₂((m+n)/(f(v)+1))\n                  + Σ_v log₂((m+n)/(f(v)+1)))')}
 <p>频繁访问的键有更大的分析权重，因而较小的每次访问界。这是明确保留启动开销的频率推论；本式不是省略前提后的“任意短序列与最佳静态树总成本恒定竞争”声明。</p>
 <h3>静态手指：距离固定位置近的访问更便宜</h3><p>固定一个中序排名 f，令 d(v)=|rank(v)−f|，选 w(v)=1/(d(v)+1)²。每个距离最多两个节点，且 Σ_{j≥1}1/(j+1)² ≤ Σ_{j≥1}1/[j(j+1)]=1，所以 W≤3。</p>${eq('log₂(W/w(x)) ≤ log₂3 + 2log₂(d(x)+1)\n总成本 = O(m + Σ_i log₂(d(x_i)+1) + n log(n+1))')}
 <p>最后一项来自每个节点 W/w(v)≤3n²。这里手指 f 在整个序列中固定；若把 f 偷换成上次访问位置，权重会变化，必须分析额外势能变化，不能用本段证明直接推出动态手指或工作集定理。本页不把这些更强的序列定理当成基本操作证明的必要步骤。</p>`,
 audit:`<h3>四个可以自行复算的问题</h3>
 <details><summary>1. 根为 3 的左链 3←2←1，伸展 1，势能一定下降吗？</summary><p>前后均为三节点链，大小集合都是 {1,2,3}，Φ=log₂6，ΔΦ=0。旋转成本 2，均摊成本也是 2；上界为 3log₂3。势能不要求每次伸展都下降。</p></details>
 <details><summary>2. 7 个节点时插入新叶，为什么长路径不产生 Θ(n) 的势能增量？</summary><p>祖先旧大小是 {1,…,7} 的子集，乘积 ∏(s+1)/s ≤ 8，所以势能增量 ≤ log₂8=3。若初始为链且在最深端插入，祖先大小恰为 1,…,7，取等号。</p></details>
 <details><summary>3. 左树 l=2、右树 r=6，接边到底增加多少势能？</summary><p>左树最大值已经伸展到根后，接边仅使该根大小由 2 变为 8：ΔΦ=log₂(8/2)=2。左树伸展本身的成本和势能变化须另计。</p></details>
 <details><summary>4. 负的均摊成本是否意味着程序倒着运行？</summary><p>不是。真实成本非负；ĉ=c+ΔΦ 可以为负，说明本步释放的势能超过旋转成本。总账仍是 Σc=Σĉ+Φ₀−Φ_m。</p></details>
 <p>证明审查时依次确认：实现约定是否一致；搜索是否伴随伸展；双旋是否按整轮；权重是否固定；森林是否完整保留；新增／移除的势能是否计入；初始项是否保留；真实时间是否已包含输出、push/pull 和回收。</p>
 <h3>原始论文与课程资料</h3><p>经典 Splay 与访问引理的来源是 <a href="https://www.cs.cmu.edu/~sleator/papers/self-adjusting.pdf" target="_blank" rel="noopener">Sleator–Tarjan，Self-Adjusting Binary Search Trees，JACM 1985</a>。可结合 <a href="https://courses.csail.mit.edu/6.854/21/Notes/n03-splay.html" target="_blank" rel="noopener">MIT 6.854，Splay Trees notes</a> 与 <a href="https://courses.csail.mit.edu/6.854/17/Scribe/s3-splay.html" target="_blank" rel="noopener">MIT 6.854，Lecture 3 scribe notes</a> 核对定义。本文的分阶段账本、边界接口、数值实验与逐行教学推演为课程网站编写。</p>`
};
